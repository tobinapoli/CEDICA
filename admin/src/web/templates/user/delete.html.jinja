<h2>Eliminar usuario</h2>
<p>¿Estás seguro de que quieres eliminar a {{ user.username }}?</p>

<form method="POST">
  {{ form.csrf_token }}
  <button type="submit" class="btn btn-danger">Eliminar</button>
  <a href="{{ url_for('user.index') }}" class="btn btn-secondary">Cancelar</a>
</form>